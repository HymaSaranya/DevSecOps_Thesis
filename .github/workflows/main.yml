name: DevSecOps Workflow

on:
  push:
    branches:
      - main

jobs:
  security_scan:
    name: Security Scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Install Dependencies
        run: |
          pip install semgrep bandit
          npm install -g codeclimate-test-reporter snyk

      - name: Run Semgrep
        run: semgrep -o semgrep-report.html --config=.semgrep.yml .

      - name: Run Bandit
        run: bandit -r . -f html -o bandit-report.html || true

      - name: Run CodeClimate
        run: |
          codeclimate-test-reporter format-coverage -t coverage.py -o codeclimate.html
        env:
          CODECLIMATE_REPO_TOKEN: ${{ secrets.CODECLIMATE_REPO_TOKEN }}

      - name: Run Snyk
        run: snyk test --all-projects --json > snyk-report.json

      - name: Run SonarCloud
        run: |
          sonar-scanner \
            -Dsonar.organization=hymasaranya \
            -Dsonar.projectKey=hymasaranya \
            -Dsonar.sources=.

      - name: Upload Semgrep Report
        uses: actions/upload-artifact@v2
        with:
          name: semgrep-report
          path: semgrep-report.html

      - name: Upload Bandit Report
        uses: actions/upload-artifact@v2
        with:
          name: bandit-report
          path: bandit-report.html

      - name: Upload CodeClimate Report
        uses: actions/upload-artifact@v2
        with:
          name: codeclimate-report
          path: codeclimate.html

      - name: Upload Snyk Report
        uses: actions/upload-artifact@v2
        with:
          name: snyk-report
          path: snyk-report.json

      - name: Upload SonarCloud Report
        uses: actions/upload-artifact@v2
        with:
          name: sonarcloud-report
          path: .scannerwork/report-task.txt
